apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-dev
  labels:
    environment: dev
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/dev-data
  storageClassName: manual

