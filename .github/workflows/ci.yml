name: Basic CI
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Run Python
      run: |
        echo "Starting tests..."
        
        # Простой тест без сложного синтаксиса
        python3 -c "
print('🎉 Testing...')
assert 1 + 1 == 2
print('✅ Basic test passed')

# Try to import calculator
try:
    import calculator
    print('✅ Calculator module found')
except:
    print(' Calculator not found')

print('🎊 Done!')
        "
